#include <stdio.h>
#include <math.h>

int main(){
    double x=1/293;
    int y = 640320^3+744;
    double a =4*(4*atan(0.2)-atan(x));
    double b= log(y)/sqrt(163);
    printf("%.100f\n", a);
    printf("%.100f\n", b);

    return 0;
}